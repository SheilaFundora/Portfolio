import React from 'react';

const DatesAndPlace = ({info}) => {
    return (
        <div className={'my-2'}>
            <span className={'test-resume-style'}>{info}</span>
        </div>
    );
};

export default DatesAndPlace;