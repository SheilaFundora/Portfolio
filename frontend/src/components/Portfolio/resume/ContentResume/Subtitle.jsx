import React from 'react';

const Subtitle = ({text}) => {
    return (
        <div>
            <p className={'test-resume-style'}>
                {text}
            </p>
        </div>
    );
};

export default Subtitle;
